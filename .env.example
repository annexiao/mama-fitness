# Copy this file to `.env` and fill in real values. `.env` is gitignored.
# Add one variable per line. Group related variables with a blank line between groups.

# Example:
# OPENAI_API_KEY=sk-...
# DATABASE_URL=postgresql://localhost:5432/mydb
